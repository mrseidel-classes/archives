package recursionpkg;

public class SumDigits implements RecursiveInterface {
	
	/*
	 
	 	You are to create a recursive (no loops) and an iterative (loops) version of the problem below
	 
	 
		Given a non-negative int n, return the sum of its digits. 
		Note that mod (%) by 10 yields the rightmost digit (126 % 10 is 6), 
		while divide (/) by 10 removes the rightmost digit (126 / 10 is 12). 

		recursiveCalculate(126) -> 9
		recursiveCalculate(49) -> 13
		recursiveCalculate(12) -> 3

		calculate(126) -> 9
		calculate(49) -> 13
		calculate(12) -> 3
	 */
}
